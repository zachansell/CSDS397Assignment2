To ingest the raw data from the csv file, execute the following SQL statement after inserting the file pathway:

BULK INSERT employee_data_source
FROM '~insert file pathway here~'
WITH
(
    FIRSTROW = 2,
    FIELDTERMINATOR = ',',
    ROWTERMINATOR = '\n'
)
